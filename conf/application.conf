# This is the main configuration file for the application.
# ~~~~~

application.secret="holahola"
application.langs="en"

# The blogs
blogs {
  allthatjazz {
    order = 10
    path = ""
    gitConfig {
      gitRepo = "./blogs/allthatjazz"
      branch = "master"
      remote = "origin"
    }
  }
  ropedin {
    path = "/ropedin"
    gitConfig {
      gitRepo = "./blogs/ropedin"
      branch = "published"
      remote = "origin"
    }
  }
}